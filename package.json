{
  "name": "@maja.id/snap-core",
  "version": "1.0.7",
  "description": "BI SNAP API SDK",
  "scripts": {
    "postinstall": "npm run build",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "author": "Maja <support@maja.id>",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.11",
    "release-it": "^17.2.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "class-validator": "^0.14.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
